import { Navbar } from './components/Navbar';

export const App = () => {
  return (
    <main className=" bg-bg text-text min-h-screen transition-all">
      <Navbar />
    </main>
  );
};

export default App;
